# Frontend

React and TypeScript application shell for the AI Quant Research Platform.
Dashboard features are intentionally deferred until Phase 6.

Run commands from this directory:

```sh
npm ci
npm run dev
npm run lint
npm run typecheck
npm test
npm run build
```
